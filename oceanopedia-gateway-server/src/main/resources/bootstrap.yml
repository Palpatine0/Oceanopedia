server:
  port: 4007

spring:
  application:
    name: oceanopedia-gateway-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
          lower-case-service-id: true

      routes:

    config:
      uri: http://localhost:8764
      label: master
      name: oceanopedia
      profile: dev